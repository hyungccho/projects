<h1><%= @band.name %></h1>
<br>
<h3>Albums</h3>
<%= render partial: 'albumlist' %>
<%= link_to 'Create an Album', new_band_album_url(@band) %>
<br>

<%= link_to 'Edit Band', edit_band_url(@band) %>
<br>

<%= button_to 'Delete Band', band_url(@band), method: :delete %>

<%= link_to 'Back', bands_url %>

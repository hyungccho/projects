<h1><%= @track.title %></h1>

<pre><%= uglify(@track.lyrics) %></pre>

<%= render partial: 'notes/note' %>

<%= link_to 'Edit Track', edit_track_url(@track) %>
<%= link_to 'Add Note', new_track_note_url(@track) %>
<%= link_to 'Back', album_url(@track.album_id) %>

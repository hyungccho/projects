<h1><%= @album.title %></h1>

<h3>Tracks</h3>
<%= render partial: 'tracklist' %>
<%= link_to "Create a Track", new_album_track_url(@album) %>
<br>

<%= link_to 'Edit Album', edit_album_url(@album) %>
<br>

<%= button_to 'Delete Album', album_url(@album), method: :delete %>
<br>

<%= link_to 'Back', band_url(@album.band_id) %>
